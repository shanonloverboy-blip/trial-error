# Fine-grained GitHub personal access token, scoped to this repo only,
# with Contents: Read and write permission. Used as the app's database:
# every submission commits an updated data/state.json to the "data" branch.
GITHUB_TOKEN=

# PIN/password checked server-side to unlock the Dashboard & Gaji tab.
DASHBOARD_PIN=2580
